apiVersion: apps/v1
kind: Deployment
metadata:
  name: swkb-front-deployment
  labels:
    app: {{ .Values.AppName }}
    chart: helm-swkb
    release: {{ .Release.Service }}
    heritage: {{ .Release.Service }}
spec:
  replicas: {{ .Values.Replicas }}
  selector:
    matchLabels:
      app: {{ .Values.AppName }}
      release: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ .Values.AppName }}
        release: {{ .Release.Name }}
    spec:
      containers:
        - name: {{ .Values.AppName }}
          image: {{ .Values.image }}   
          ports:
            - containerPort: {{ .Values.ContainerPort }}
          livenessProbe:
            httpGet:
              path: /
              port: {{ .Values.ContainerPort}}
          readinessProbe:
            httpGet:
              path: /
              port: {{ .Values.ContainerPort }}